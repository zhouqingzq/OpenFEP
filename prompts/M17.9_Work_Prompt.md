# OpenFEP M17.9 Work Prompt

## Milestone

M17.9: Local Field Potential And Non-TopK Recall

## Goal

Upgrade the path-first substrate into a bounded local field approximation so
the runtime can do something stronger than fixed top-k recall.

Target contract:

```text
coarse bounded retrieval proposes a local neighborhood
  -> local field owner builds a bounded potential surface from path objects
  -> decision follows basin / ridge / gradient summaries
  -> best-single and naive top-k counterfactuals are audited
```

This milestone must not merely add another ranking weight. It must establish a
new local decision object that can beat both:

```text
best single memory
naive additive top-k aggregate
```

when the local field geometry matters.

## Architecture Position

```text
coarse retrieval top-N
  -> local field neighborhood builder
  -> potential / gradient / basin summary
  -> world-model prediction refinement
  -> counterfactual audit against best-single and naive top-k
```

Recommended landing points:

```text
segmentum/memory_retrieval.py
segmentum/agent.py
segmentum/world_model.py
segmentum/memory_store.py
```

If a new owner module is introduced, prefer:

```text
segmentum/memory_field.py
```

## Dependencies

Hard:

- M17.8 path-first memory substrate
- M17.5 canonical FE vocabulary
- Existing retrieval and counterfactual audit surfaces

Soft:

- Existing M17.4 anti-top-k reasoning style as a design reference
- Existing bundle / recall diagnostics

Compatibility-sensitive:

- Existing coarse top-k retrieval owner contracts
- Existing downstream consumers that assume a single winning memory
- Existing retrieval traces that do not distinguish neighborhood proposal from
  final influence

## Non-Goals

- Do not run unbounded all-memory field simulation.
- Do not invent physics metaphors without measurable bounded outputs.
- Do not claim field behavior if best-single or naive top-k would already
  explain the same decision.
- Do not let the LLM define potential values or gradients.
- Do not touch Path A or M10.

## Required Work

### 1. Add a bounded local field contract and owner

Define a compact object such as:

```text
LocalMemoryField:
  field_id
  member_path_ids
  member_memory_ids
  potential_by_channel
  gradient_by_channel
  basin_strength
  ridge_strength
  conflict_zones
  field_confidence
```

Rules:

- The field is local to a bounded neighborhood only.
- Members must come from coarse retrieval or path-neighborhood rules, not from
  arbitrary global search.
- The field must be derived from deterministic path and quality signals.
- Potential values must have channel-level meaning, not merely one more opaque
  scalar score.

Recommended diagnostic fields:

```text
effective_member_count
dominant_path_share
synergy_margin
gradient_magnitude
field_flatness
conflict_density
```

### 2. Make coarse retrieval a neighborhood proposal only

Current top-k retrieval may remain as stage 1, but only as:

```text
candidate neighborhood proposal
not final memory decision owner
```

The implementation must explicitly separate:

```text
coarse retrieval score
field potential summary
downstream chosen influence
```

The implementation must preserve a named owner split:

```text
coarse retrieval owner   -> bounded neighborhood proposal
local field owner        -> potential / geometry summary
downstream consumer      -> uses field summaries, not raw top-k rank alone
```

### 3. Add deterministic local field construction rules

The field builder must specify how it converts path-level signals into bounded
potential geometry.

Required ingredients:

```text
path quality or utility
contradiction burden
risk profile
expected surprise profile
goal-channel compatibility, if available
```

Rules:

- contradictory members must increase ridge or conflict structure rather than
  silently average out
- duplicates or near-duplicates must not fake a richer field
- field confidence must decrease in flat, sparse, or highly contradictory local
  neighborhoods

### 4. Add at least one field-based downstream consumer

At least one live runtime path must consume field outputs such as:

```text
prediction refinement from basin pull
danger / safety channel adjustment from ridge and caution corridors
reply-policy or action-policy shift from local conflict zones
```

The consumer must read field summaries, not only the best single path.

First shipped version should pick one reachable consumer in the ordinary runner
path rather than a hidden diagnostics-only consumer.

### 5. Add explicit anti-top-k counterfactuals

For every field-triggered decision class, compute:

```text
best_single_counterfactual
naive_topk_counterfactual
field_enabled_decision
```

If either counterfactual would already trigger the same decision under the same
budget, the runtime must audit the case as top-k-equivalent.

### 6. Add bounded field metrics and decision audit

The local field should expose measurable diagnostics such as:

```text
effective_member_count
dominant_path_share
synergy_margin
gradient_magnitude
field_flatness
conflict_density
```

These will be needed by M17.10 adaptive compute.

Required event family:

```text
LocalFieldBuiltEvent
LocalFieldDecisionEvent
LocalFieldSuppressedEvent
```

### 7. Add deterministic fixtures and negative controls

There must be at least one fixture where:

```text
no single path crosses threshold
naive top-k is insufficient or contradictory
field summary changes the bounded downstream decision
```

And at least one negative control where:

```text
best single already explains the decision
or naive top-k additive aggregation already explains it
```

In those cases the runtime must suppress the "field-required" claim.

## File Touch List

```text
segmentum/memory_field.py                           (new, recommended)
segmentum/memory_retrieval.py                       (neighborhood proposal split)
segmentum/memory_store.py                           (path/member access helpers)
segmentum/world_model.py                            (field-based prediction input)
segmentum/agent.py                                  (field consumer wiring)
segmentum/tracing.py                                (field audit events)
tests/test_m17_9_local_memory_field.py              (new)
fixtures/m17_9/**                                   (new)
```

## Tests

Add tests for:

```text
test_coarse_retrieval_is_only_neighborhood_proposal
test_local_field_builds_from_multiple_paths_without_global_search
test_duplicate_members_do_not_fake_richer_field
test_conflicting_members_raise_conflict_density_or_ridge_strength
test_field_consumer_reads_field_summary_not_only_best_single_path
test_field_required_decision_suppressed_when_best_single_would_trigger
test_field_required_decision_suppressed_when_naive_topk_would_trigger
test_local_field_emits_geometry_diagnostics
test_local_field_can_change_bounded_downstream_decision
```

## Acceptance Criteria

M17.9 is complete when:

1. The runtime has a bounded local field owner separate from coarse top-k
   retrieval.
2. Coarse retrieval is explicitly only a neighborhood proposal stage.
3. At least one live consumer uses field summaries rather than only the
   strongest single path.
4. Best-single and naive top-k counterfactuals are computed for field-triggered
   decisions.
5. The runtime only claims field-required behavior when both counterfactuals
   fail under the same budget.
6. The field exposes bounded geometry-style diagnostics such as gradient,
   flatness, or conflict density.
7. Path A and M10 remain untouched.

## Verification Standard

Use one fixture where:

```text
several locally related paths are retrieved
no single path crosses the downstream decision threshold
naive top-k additive support is flat or internally contradictory
```

Expected:

```text
the local field owner builds a bounded neighborhood field
field geometry changes the downstream decision
best_single_counterfactual == false
naive_topk_counterfactual == false
```

Use a second fixture where one dominant path already explains the outcome.

Expected:

```text
the system emits a suppression or top-k-equivalent audit event
it does not count the case as field-required behavior
```
